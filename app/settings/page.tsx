import { AppShell } from "@/components/shell"; import { Card, PageHeader } from "@/components/ui";
import { requirePageUser } from "@/lib/auth/page-user";
export default async function Settings(){const user = await requirePageUser("/settings");return <AppShell title="Settings" user={user}><PageHeader eyebrow="Your controls" title="Settings" subtitle="Set the boundaries SalesPilot must respect. Everything else is handled quietly in the background."/><div className="grid cols-2"><Card><div className="card-title">Campaign control</div><div className="card-subtitle">Choose how much approval newly launched campaigns require.</div><div className="field section"><label>Default mode</label><select className="select"><option>Approval mode</option><option>Automatic mode</option><option>Assisted mode</option></select></div><div className="field section"><label>Automatic action standard</label><input className="input" value="High confidence only" readOnly/></div></Card><Card><div className="card-title">Sending hours</div><div className="card-subtitle">Initial outreach follows each recipient's local working day.</div><div className="form-grid section"><div className="field"><label>Start</label><input className="input" value="08:00" readOnly/></div><div className="field"><label>End</label><input className="input" value="18:00" readOnly/></div></div><p className="card-subtitle">SalesPilot uses the contact's work location first, then the company location. Uncertain cases are held for review.</p></Card><Card><div className="card-title">Decision transparency</div><div className="card-subtitle">Important recommendations include a clear explanation, supporting evidence and an easy way to edit or reject them.</div><div className="section"><span className="badge green">Explanations enabled</span></div></Card><Card><div className="card-title">Connections</div><div className="card-subtitle">Email, calendar and data connections will be configured here when live integrations are enabled.</div><div className="section"><button className="button secondary">Configure later</button></div></Card><Card><div className="card-title">Product information</div><div className="card-subtitle">Current SalesPilot production milestone.</div><div className="section"><span className="badge green">Genesis G1.2 · Production</span></div></Card></div></AppShell>}
