import { NextResponse } from "next/server"; import { z } from "zod"; import { requireOrganisationContext } from "@/lib/auth/organisation-context"; import { databaseRequest } from "@/lib/database/postgrest";
const Schema=z.object({campaignId:z.string().uuid(),status:z.enum(["APPROVED","REJECTED","HOLD","ARCHIVED","PENDING_REVIEW"]),note:z.string().trim().max(500).optional()});
export async function POST(request:Request,{params}:{params:Promise<{id:string}>}){try{const {id}=await params;const input=Schema.parse(await request.json());const context=await requireOrganisationContext();await databaseRequest("rpc/review_salespilot_contact_scoped",{method:"POST",body:JSON.stringify({p_organisation_id:context.organisationId,p_campaign_id:input.campaignId,p_contact_id:id,p_user_id:context.userId,p_status:input.status,p_note:input.note??null})});return NextResponse.json({ok:true})}catch(error){console.error("Contact review failed",error);return NextResponse.json({ok:false},{status:400})}}
