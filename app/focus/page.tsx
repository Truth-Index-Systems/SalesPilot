import { AppShell } from "@/components/shell"; import { Card, PageHeader } from "@/components/ui"; import { AlertTriangle, CheckCircle2, Mail, Target } from "@/components/icons";
const tasks=[{icon:Mail,title:"Approve 8 initial messages",why:"Average Truth Index 91 · all emails verified",action:"Review batch",tone:"green"},{icon:Target,title:"Prepare for Northline discovery call",why:"Meeting Tuesday 14:30 · estimated value £32k",action:"Open brief",tone:"green"},{icon:AlertTriangle,title:"Resolve Atlas mailbox warning",why:"Authentication expires in 3 days",action:"Reconnect",tone:"amber"}];
export default function Focus(){return <AppShell title="Focus Mode"><PageHeader eyebrow="Exception-only workspace" title="Your highest-value actions" subtitle="Routine work stays in the background. Focus Mode contains only decisions that require human judgement or protect revenue."/><div className="hero"><div className="eyebrow" style={{color:"#d8f6ff"}}>Today's potential impact</div><h2>Three actions can protect or advance £74,000 of pipeline</h2><p>SalesPilot has completed the research and preparation. Your role is to make the decisions only you should make.</p></div><div className="grid section">{tasks.map(({icon:Icon,...t})=><Card key={t.title}><div style={{display:"flex",gap:14,alignItems:"center"}}><div className="card soft"><Icon size={21}/></div><div style={{flex:1}}><div className="name">{t.title}</div><div className="meta">{t.why}</div></div><button className="button primary">{t.action}</button><CheckCircle2 size={18} className="positive"/></div></Card>)}</div></AppShell>}
