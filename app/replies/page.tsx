import { AppShell } from "@/components/shell"; import { Card, PageHeader } from "@/components/ui";
import { requirePageUser } from "@/lib/auth/page-user";
const replies=[{name:"Sarah Mitchell",company:"Northline Distribution",type:"Meeting requested",message:"Happy to see this. Can you do Tuesday afternoon?",action:"Propose 14:30 Tuesday",score:"97%"},{name:"David Cole",company:"Atlas Fulfilment",type:"Positive question",message:"How would this connect with our current CRM?",action:"Answer integration question",score:"93%"},{name:"Priya Shah",company:"Redwood Services Group",type:"Not now",message:"Revisit this after our Q4 planning cycle.",action:"Schedule October follow-up",score:"91%"}];
export default async function Replies(){const user = await requirePageUser("/replies");return <AppShell title="Engagement" user={user}><PageHeader eyebrow="Opportunity engagement" title="Engagement" subtitle="Approved opportunities move here. SalesPilot will prepare outreach, understand replies and recommend the next action across the full conversation journey."/><div className="grid">{replies.map(r=><Card key={r.name}><div className="section-head"><div><div className="name">{r.name} · {r.company}</div><div className="meta"><span className="badge green">{r.type}</span> · {r.score} confidence</div></div><button className="button primary">{r.action}</button></div><p>“{r.message}”</p><div className="recommendation"><h3>Recommended next step</h3><p>{r.action}. The reply intent is clear and the recommendation is grounded in the prospect&apos;s exact request.</p></div></Card>)}</div></AppShell>}
